自监督学习通过设计代理任务，如掩码建模~\cite{Xie00LBYD022,HeCXLDG22}（Masked Image Modelling）
和对比学习~\cite{CaronTMJMBJ21,OquabDMVSKFHMEA24,Simeoni25}（Contrastive Learning），
从没有标注的数据中学习更加通用的表征模式。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\linewidth]{figures/chap2.7-inside-vit.png}
    \caption{视觉自注意力编码器内部结构}
    \label{fig:2.7-inside-vit}
\end{figure}

这是一个测试混合中文和 LaTeX 命令的文档。

一些常见的 LaTeX 命令示例：

1. 引用命令：参考文献~\cite{Author2023}是很重要的。
2. 粗体命令：使用\textbf{粗体文字}来强调重点。
3. 斜体命令：使用\textit{斜体文字}来表示术语。
4. 数学公式：行内公式 $E=mc^2$ 和行间公式。
5. 环境命令：\begin{itemize} 和 \end{itemize}。

测试更多复杂的情况：

图像分类任务使用\cite{He2016}提出的 ResNet 模型，效果很好。
自然语言处理中\cite{Vaswani2017}提出的 Transformer 架构已经成为主流。

章节标题示例：

\section{引言}
\subsection{研究背景}
\subsubsection{相关工作}

列表环境示例：

\begin{enumerate}
    \item 第一项内容
    \item 第二项内容
    \item 第三项内容
\end{enumerate}

图表引用示例：

如图~\ref{fig:example}所示，模型性能有显著提升。
详见表~\ref{tab:results}中的实验结果。

数学公式示例：

\begin{equation}
    f(x) = \int_{0}^{\infty} e^{-x^2} dx
\end{equation}

定理环境示例：

\begin{theorem}
    这是一个定理的内容。
\end{theorem}

代码块示例：

\begin{lstlisting}[language=Python]
def hello_world():
    print("Hello, World!")
\end{lstlisting}

结论：LaTeX 命令应该被正确保护，不会在命令和参数之间添加空格。
